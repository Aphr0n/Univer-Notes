Кодування Хаффмана – це форма статистичного кодування. Розроблено доктором Девідом А. Хаффманом у 1952 році – метод побудови кодів з мінімальною надлишковістю. Застосовується до багатьох форм передачі даних, наприклад факс.
# Загальна ідея
У стандартних кодуваннях (наприклад, ASCII) кожен символ займає однакову кількість бітів  
(зазвичай 1 символ = 8 біт), незалежно від того, як часто він зустрічається.
Однак у реальних даних символи з’являються з **різною частотою**:
- деякі — дуже часто (пробіл, голосні),
- деякі — дуже рідко.
# Основна ідея алгоритму
1. Сканувати текст, який потрібно стиснути, та підрахувати кількість входжень усіх символів.
2. Сортувати або визначити пріоритети символів на основі кількості входжень у тексті.
3. Створити дерево коду Хаффмана на основі списку пріоритетів.
4. Пройтися по дереву, щоб визначити всі кодові слова.
5. Знову просканувати текст та створити новий файл, використовуючи код Хаффмана.

# Створення бінарного кореневого дерева – пріоритезація символів
1. Створення листків бінарного дерева для кожного символу та за його частотою.
2. Розміщення вершин у черзі пріоритетів.
3. Чим нижча вхідність, тим вищий пріоритет у черзі.
4. Якщо черга пріоритетів містить дві або більшевершин, тоді виконайте такі дії:
	- Створіть новий вузол.
	- Виберіть вершину з черги та змініть її на ліве піддерево.
	- Виберіть інший вузол з черги та змініть його на праве піддерево.
	- Частота нового вузла дорівнює сумі частот лівого та правого нащадків.
	- Поверніть новий вузол у чергу.

# Кодування файлу – Дерево обходу для кодів:
1. Обхід бінарного дерева для отримання нових кодових слів, починаючи з кореня бінарного дерева.
2. Зсув ліворуч дорівнює 0.
3. Зсув праворуч дорівнює 1.
4. Кодове слово вважається завершеним лише тоді, коли досягнуто останньої вершини (листка бінарного дерева – рота).

# Приклад
## Приклад побудови дерева Хаффмана
Нехай маємо такий текст і частоти символів:

| Символ | Частота |
| ------ | ------- |
| A      | 5       |
| B      | 2       |
| C      | 1       |
| D      | 1       |

## Крок 1. Створюємо листки
Для кожного символу створюємо вузол:
`A(5), B(2), C(1), D(1)`
## Крок 2. Поміщаємо в пріоритетну чергу
Пріоритет — **менша частота = вищий пріоритет**:
`C(1), D(1), B(2), A(5)`
## Крок 3. Основний цикл
### 🔹 Ітерація 1
Беремо два найменші:
- `C(1)` → лівий піддерев
- `D(1)` → правий піддерев
Створюємо новий вузол:
`CD(2)`
Черга тепер:
`B(2), CD(2), A(5)`
### 🔹 Ітерація 2
Беремо два найменші:
- `B(2)`
- `CD(2)`
Створюємо новий вузол:
`BCD(4)`
Черга тепер:
`BCD(4), A(5)`
### 🔹 Ітерація 3
Беремо два останні вузли:
- `BCD(4)`
- `A(5)`
Створюємо корінь:
`ABCD(9)`
Черга містить **один вузол** → алгоритм завершено.
## Отримане дерево Хаффмана

```
          (9)
         /   \
      (4)     A(5)
     /   \
  B(2)   (2)
        /   \
     C(1)   D(1)
```
## Крок 4. Призначаємо коди
- ліве ребро → `0`
- праве ребро → `1`
Отримуємо:

|Символ|Код|
|---|---|
|A|`1`|
|B|`00`|
|C|`010`|
|D|`011`|
