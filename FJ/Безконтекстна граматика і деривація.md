Безконтекстна граматика — це четвірка:

```text
G = (N, T, P, S)
```

- `N` – множина нетерміналів  
- `T` – множина терміналів  
- `P` – множина продукційних правил  
- `S ∈ N` – стартовий символ  

## Продукції

Правила мають вигляд `A → α`, де:
- `A ∈ N`
- `α ∈ (N ∪ T)*`