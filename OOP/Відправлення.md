**Відправлення (dispatch)** в об’єктно-орієнтованому програмуванні — це механізм **вибору, яку саме реалізацію методу викликати**

# Ключові поняття
## 1. **Apparent type (статичний / оголошений тип)**

Тип змінної, який відомий **під час компіляції**. Є незмінним

```Java
Article a;   // apparent type — Article
```

## 2. **Actual type (динамічний / реальний тип)**

Тип об’єкта, який реально зберігається у змінній **під час виконання**. Він завжди є підтипом apparent type

```Java
a = new DiscountArticle(); // actual type — DiscountArticle
```
# Динамічне відправлення
**Динамічне відправлення** — це правило:
> 🔹 **Який метод буде викликаний, вирішується за actual type об’єкта, а не за apparent type змінної.**