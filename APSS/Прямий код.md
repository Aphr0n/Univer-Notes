Прямий код — це спосіб подання знакових цілих чисел у двійковій системі.
Структура N-бітового прямого коду:

$[ знаковий біт ][ біти модуля (величини) ]$
1 знаковий біт — найлівіший
N−1 біт — модуль числа (його абсолютне значення)
## 🔹 Знаковий біт
Позначається як `N_sign`:
- `0` → число **додатне** або **0**
- `1` → число **від’ємне**

+6 <-> 0110

## 🔹 Основні недоліки прямого коду

### ❌ 1. Додавання працює неправильно

Приклад:
`-6 + 6 1110 +0110 ----- 10100   ← неправильний результат`
Причина:
- апаратне додавання не “розуміє” знак
- треба спеціальна логіка для обробки знакових бітів
### ❌ 2. Дві різні нулі
У прямому коді існують **дві нулі**:
- `0000` → +0
- `1000` → −0
Це дуже незручно:
- порівняння
- арифметика
- логіка процесора ускладнюється